name: Deploy

on:
  push:
    branches:
      - production
  pull_request:
    branches:
      - production
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      uses: actions/checkout@v2
    - name: Build
      run: |
        npm install
        npm run build